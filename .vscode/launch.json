{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run reverie backend_server",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/reverie/backend_server/reverie.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/reverie/backend_server"
        }
    ]
}
